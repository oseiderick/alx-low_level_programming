#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: 0 Always (success)
 */
int main(void)
{
	for (char c = 'a'; c <= 'z'; c++)
	{
		putchar(c);
	}
	/*prints out the new line*/
	putchar('\n');

	return (0);


}
